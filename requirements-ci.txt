# Dependencies for the cloud refresh (GitHub Actions). Everything else build.py
# needs is in the Python standard library.
openpyxl                 # read the capacity workbook (.xlsx)
pymupdf                  # extract reflection-report text (build.py imports fitz)
google-api-python-client # fetch the workbook + reflections from Google Drive
google-auth              # service-account auth for the Drive fetch
